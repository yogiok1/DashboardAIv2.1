import "./bootstrap";
import Swal from "sweetalert2";

window.Swal = Swal;

// Do NOT import Alpine here - let Livewire handle it completely
// This prevents the "Detected multiple instances of Alpine" warning
